
<a class = "p-4" href="/">Create Post</a>
        
        <h2 class="title pb-4">dashboard: your posts</h2><hr>
{{#if posts.length}}
    </div>

    {{#each posts as |post|}}
        {{> post-info post}}
        
            <a href="/dashboard/edit/{{post.id}}"><button class="">edit post</button></a>
        </div>
    {{/each}}

{{else}}
<h3 class="pb-4 has-text-centered is-italic">you do not currently have any posts</h3>

{{/if}}
